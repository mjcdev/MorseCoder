<Page
    x:Class="MorseCoder.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MorseCoder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{Binding BackgroundBrush}"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid Background="{Binding BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="149*"/>
            <RowDefinition Height="171*"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer>
            <TextBlock TextWrapping="Wrap" Text="{Binding Translation}" FontSize="48" Margin="10"/>
        </ScrollViewer>
        
        <TextBox Text="{Binding Input, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="26" Margin="10" TextWrapping="Wrap" Grid.Row="1"/>
        
    </Grid>
</Page>